const conversion = @import("conversion.zig");
const VectorAlignedSlice = conversion.VectorAlignedSlice;

pub fn findIndex(comptime T: type, slice: VectorAlignedSlice(T)) usize {
    //todo
    _ = slice;
    return 0;
}
